
CREATE TABLE IF NOT EXISTS pdfs(
  id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
  titulo VARCHAR(100) NOT NULL,
  trilogia VARCHAR(100) NOT NULL,
  autor VARCHAR(100) NOT NULL,
  livro VAHCAR(5) NOT NULL
)
